tạo mới kho chứa
--git init
kiểm tra trạng thái
-- git status
them file 
-- git add "ten file"
thêm tất cả file
-- git add .
chú thích thay đổi
-- commit -m "..."
gộp cả thêm cả chú thích
-- commit -a -m ".."
tra cứu lịch sử commit
-- git log


github
tạo mooy repo mới
làm theo hướng dẫn
tạo Branch
git branch -M (ten)
git remote add origin (đường link)
git push -u origin (master/tên branch)


tai ve may tinh
tim vị trí lưu -> chuột phải + git bash here
git clone + duong dan copy

sửa file -> git push :đẩy file lên github

quay lại lần sửa trc
git revert + id

cập nhật từ gihub về
-- git pull
sang nguyen 

